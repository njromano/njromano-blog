{
  "name": "njromano-blog",
  "version": "1.0.0",
  "dependencies": {
    "@nuxt/content": "^1.14.0",
    "@tailwindcss/typography": "^0.4.0",
    "fs": "0.0.1-security",
    "net": "^1.0.2",
    "nuxt": "^2.15.3"
  },
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "generate": "nuxt generate && git add dist && git commit -m \"run nuxt generate\"",
    "start": "nuxt start",
    "publish": "git subtree split --prefix dist -b gh-pages && git push -f origin gh-pages:gh-pages && git branch -D gh-pages"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^4.0.1",
    "postcss": "^8.2.8"
  }
}
